{
  "name": "equipmaster",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/src/index.js",
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf backend/public && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "postinstall": "cd backend && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
