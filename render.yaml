services:
  - type: web
    name: equipmaster
    runtime: node
    plan: free
    buildCommand: npm install --prefix frontend && npm run build --prefix frontend && rm -rf backend/public && cp -r frontend/dist backend/public && npm install --prefix backend
    startCommand: node backend/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
