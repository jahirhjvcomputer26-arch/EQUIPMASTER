services:
  - type: web
    name: equipmaster
    runtime: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build && cp -r dist ../backend/public
    startCommand: node backend/src/index.js
    envVars:
      - key: NODE_ENV
        value: production
