PORT=3001
CORS_ORIGINS=http://localhost:3001,http://localhost:5173,http://192.168.100.182:3001
JWT_SECRET=equipmaster-jv-computer-2026
FIREBASE_DB_URL=https://inventarioequip-default-rtdb.firebaseio.com

# Modo SQL Server (reemplaza a Firebase cuando esté configurado)
# DB_SERVER=localhost\SQLEXPRESS
# DB_PORT=1433
# DB_DATABASE=EquipMaster
# DB_USER=equipmaster
# DB_PASSWORD=tu_contraseña
# USE_FIREBASE=1   # (opcional) fuerza el modo Firebase aunque haya DB_SERVER

# HTTPS (certificado auto-firmado)
# Ejecuta deploy/generar-cert.ps1 en el servidor para generar cert.pfx
# HTTPS_PFX=cert.pfx
# HTTPS_PASSPHRASE=equipmaster
